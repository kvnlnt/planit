'use strict';
var React = require('react');

module.exports = React.createClass({
    render: function () {
        return (
            <div ref="Menu" className="menu">
                <h1>Planit</h1>
            </div>
        );
    }
});